<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InspireMe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.InspireMe" parent="Base.Theme.InspireMe" />

    <!-- Define a reusable button style -->
    <style name="ModernButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="iconSize">26dp</item>
        <item name="iconTint">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="cornerRadius">10dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:background">@null</item>
        <item name="android:layout_marginStart">10dp</item>
<!--        <item name="android:fontFamily">@font/your_custom_font</item> &lt;!&ndash; Optional: Use a custom font &ndash;&gt;-->
<!--        <item name="android:textColor">@color/your_text_color</item> &lt;!&ndash; Optional: Set text color &ndash;&gt;-->
    </style>
</resources>